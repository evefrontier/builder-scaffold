### TypeScript Scripts
- Example TypeScript scripts to interact with contracts
- Token creation and management scripts
- Extension authorization flows
- Integration with Sui TypeScript SDK


1. Based on your environment , copy the deployed pacakge-id and paste it in the .env 
2. If its local, it uses the itemId in the constants file. If its testnet, feel free to replace the itemId and the tenant information to match the testnet resources 
3. You can run the command you want from here. 
